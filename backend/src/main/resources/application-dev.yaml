spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/easyhire
    username: postgres
    password: yash

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8080

jwt:
  secret: your-very-secure-secret-key-1234567890
  expiration: 86400000